name: Deploy to Salesforce

on:
  push:
    branches:
      - main  # Trigger the workflow on push to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Salesforce CLI
      run: |
        sudo npm install -g sfdx-cli
        sfdx --version

    - name: Authenticate Salesforce Org
      env:
        SF_USERNAME: ${{ secrets.SF_SANDBOX_USERNAME }}
        SF_PASSWORD: ${{ secrets.SF_PASSWORD }}
        SF_CONSUMER_KEY: ${{ secrets.SF_CONSUMER_KEY }}
        SF_CONSUMER_SECRET: ${{ secrets.SF_CONSUMER_SECRET }}
        SF_LOGIN_URL: https://login.salesforce.com  # Use https://test.salesforce.com for sandbox
      run: |
        sfdx auth:jwt:grant --clientid $SF_CONSUMER_KEY --jwtkeyfile server.key --username $SF_SANDBOX_USERNAME --instanceurl $SF_LOGIN_URL

    - name: Deploy to Salesforce
      run: |
        sfdx force:source:deploy -p force-app -u $SF_SANDBOX_USERNAME
